#!/usr/bin/python3
"""is_same_class - check if the object is an instance
of the specified class"""


def is_same_class(obj, a_class):
    """check if is an object is instance"""
    return (type(obj) == a_class)
